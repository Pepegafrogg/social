

const initialState = {
   friendsData: [
      { id: 1, name: 'Alexey' },
      { id: 2, name: 'Maxim' },
      { id: 3, name: 'Vladimir' },
   ]
}
const navFriendsReducer = (state = initialState, action) => {
   return state
}

export default navFriendsReducer