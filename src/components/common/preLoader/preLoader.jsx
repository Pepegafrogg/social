import React from 'react';
import preLoader from './../../../assets/images/preLoader.gif';

const PreLoader = () => {
   return (
      <div>
         <img style={{ width: '800px', margin: '0 auto' }} src={preLoader} />
      </div>
   );
}

export default PreLoader;
